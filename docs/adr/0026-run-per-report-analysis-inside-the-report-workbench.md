# 逐份规则分析归入报告工作台

“分析规则”菜单只维护规则定义、版本、条件、输出、优先级、冲突分组和测试，不再提供独立的逐份报告分析工作台；报告的规则运行、原始命中、组合结果、命中排除、人工发现和最终内容确认统一放入报告工作台。无规则命中时允许分析模块为空，存在命中时必须人工确认；检测结果、报告物种或有效参考范围变化会使旧分析失效并阻断提交。这样规则配置与报告处理各自只有一个入口，避免多处编辑产生不同最终内容。
