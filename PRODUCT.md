# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

- 管理端运营人员：确认或创建用户、在登记送检时创建并关联宠物（必填送检类型：本店送检或客户自带报告）、从该送检记录导入检测 Excel、完善报告内容，并在出错时纠错（改归宠物 / 换用户）。
- 管理端审核人员：核对专业数据和用户端呈现内容，执行发布检查并发布或退回报告。
- 小程序用户：在现有商城小程序同一 AppID 内，启动/登录默认进入健康报告首页；自送检登记起至报告发布前看到不可点击的「报告处理中」，发布后从健康报告首页进入完整报告。资料有误时联系运营，不在用户端改资料。

## Product Purpose

平台将检测机构交付的已知格式 Excel 转换为可追溯、可人工审核并适合小程序阅读的宠物健康报告。成功意味着运营先在平台写好身份再从送检记录导入结果，并逐份完成数据处理、内容完善、审核、发布、更正、作废和纠错，用户始终只看到平台当前认可的发布版本。

## Positioning

产品把实验室检测事实、审核后的有效结果和面向用户的报告呈现分层管理；分析、建议与商品推荐按菌门组织，商品由运营在单份报告的各菌门分析单元中直接选择商城 SPU，不建设推荐算法，也不把报告规则侵入商城模型。

## Operating Context

- 检测机构在线下完成检测并交付 Excel；运营先确认或创建平台用户，再创建关联该用户的宠物并登记送检（待导入结果）。登记时必填送检类型：本店送检（本店已登记样本、等待实验室 Excel）或客户自带报告（客户已在他处检测并带来文件）。两种类型走同一条管道：先选定已关联用户的宠物，再登记送检，再在该行上导入结果。用户随即看到「报告处理中」。导入结果必须从该送检记录发起，挂上一份 Excel；样本编号可从试管标签或微信补填，不要求出现在 Excel 中，也不用于自动匹配文件。
- Excel 成功解析后生成平台报告；v1 进入待完善（宠物已在送检登记时存在），再经待审核、已发布或已作废。v1 界面可操作状态只有待完善 / 待审核 / 已发布 / 已作废，另有「更正草稿进行中」和「退回原因」两个字段。不设待归属、审核退回、已通过、已更正或领取状态；模型若仍保留待归属字段，界面不得进入、也不得作为入口。不做无预登记批量导入，也不产生未挂宠物的孤儿报告。
- 编辑、完善和审核围绕同一份报告工作台完成；右侧模拟小程序用于实时预览当前工作版本，与用户端一致为单页滚动的报告阅读页（主题首屏 →「详细」滚入对比与菌门分段），不是「概览 / 对比 / 菌门」三个 tab，预览不等于发布。
- 宠物健康报告在现有商城小程序同一 AppID 下二次开发，不新建报告小程序，也不做跨小程序跳转；复用商城登录态与交易能力。启动/登录默认进入健康报告首页（当前宠物页），主导航按原型为「首页」「我的」。报告从首页打开为独立页，阅读顺序固定为等级主题首屏，底部「详细」滚入对比（仅当存在有效参考范围）再滚入菌门分段。菌门详情内按菌门呈现「分析」「总体建议」，主推商品卡跟随该菌门的建议出现（最终位置待甲方确认，见 open-questions.md Q8）。
- 真实 Excel 只有属级行、`Your sample (%)` 和 `vs AVG notice`（high / low / -），没有参考范围、摘要指标或任何编号与身份信息。文件配对已决定：导入只从已登记送检记录发起，把一份 Excel 挂到该记录上；文件名仅供人工核对，不用于自动匹配。其余数据缺口见 `docs/product-design/open-questions.md`。

## Capabilities and Constraints

- 送检管理承担报告生成前的运营工作队列，提供「登记送检」（含必填送检类型）以及挂在该送检记录上的「导入结果」入口，默认聚合待导入结果和导入异常；成功生成报告后退出默认队列并保留追溯入口。不设列表级无预登记批量导入。
- 报告中心从 Excel 成功解析并生成报告后开始，承担报告工作队列与历史档案；列表只显示报告主状态与「更正中·待完善 / 待审核」副标签。视图为待处理、全部、待完善、待审核、已发布、已作废，不设待归属页签，也不把处理归属作为行上主操作。列表不显示模块完整度或待办标记。
- 报告中心默认进入聚合待完善和待审核的“待处理”视图；待处理只含这两类，不把待归属算进去。状态数量随当前查询条件更新，工作队列优先展示滞留最久的报告，档案视图优先展示最近变化。
- 列表默认展示两到三个常用查询条件并可展开高级筛选；报告记录采用六组信息组成的紧凑记录卡，并根据状态突出唯一主要动作。
- 送检管理允许登记送检、从该送检记录导入结果和重新导入，不设独立 Excel 导入页，也不做无预登记批量导入；多文件导入仅表现为运营把 N 个文件分别挂到 N 条待导入送检记录。客户自带报告不另开「先导入再选宠物」路径。报告中心只允许逐份完善、审核、发布、作废、创建更正草稿和纠错（改归宠物 / 换用户），并提供「查看送检记录」。
- 报告工作台左侧字段与右侧小程序报告阅读页建立双向预览定位关系：当前滚动区内悬停临时定位，点击或聚焦后滚到对应分段或钻入菌门详情，多位置通过清单逐一查看，无直接展示位置时说明其后台影响。
- 右侧默认预览包含未保存修改的工作版本；直接呈现内容实时更新，专业依据变化需经确认重新分析，不静默覆盖人工调整内容。
- 工作台左右宽度可调整且预览可收起；窄屏时编辑区优先，预览进入右侧抽屉。
- 报告工作台按来源与归属 / 检测结果 / 综合评定 / 分析与建议 / 商品推荐 / 发布检查六个业务模块组织，左侧导航不把版本与记录作为一级模块。来源与归属展示已关联的送检记录与宠物 / 用户（不是从无名 Excel 选宠物的归属表单）；改归宠物 / 换用户作为纠错保留。该模块底部提供紧凑的「本报告追溯」：谁发布 / 更正 / 作废、何时，以及当前发布版与更正草稿对照。报告中心行上「更多」仍可打开版本 / 作废 / 更正草稿。分析与建议按菌门卡片逐菌门确认，商品推荐按菌门列出建议摘要与商品槽（是否并入菌门卡片待确认）。问题在模块导航和对应字段附近区分阻断与警告，待重新分析、缺失未处理、商品失效、未关联用户四类待办只以工作台角标提示；大量检测结果使用可搜索的主从编辑结构。
- 检测结果模块展示 Excel 原始值与当前有效值对照、范围来源（报告导入 / 平台配置 / 无有效范围）和实验室比较状态（high / low / 未标注），允许修改有效值并填写原因、补录缺失项；`vs AVG notice` 为 `-` 的记录仍是有效结果并正常展示。
- 页面通过“暂存”按钮把编辑内容保存为后端工作版本，暂存不改变报告状态；不以自动保存或浏览器本地草稿替代该能力。
- 暂存原子更新整份可变工作版本并记录操作人和时间；多人编辑采用后暂存覆盖先暂存，不做冲突拦截，产品接受先前修改丢失的风险。
- 提交审核先保存当前修改并执行阻断检查，待审核期间原提交人需撤回后才能继续编辑，撤回不改动送检记录；审核人员可以留痕修改，审核通过后直接发布、不设“已通过”状态，退回表达为待完善加退回原因。作废与创建更正草稿的入口在工作台顶栏（已发布时）与报告中心「更多」。
- 专业依据变化时先展示影响范围，默认只重新分析受影响菌门，同时允许重新分析整份报告；审核默认从提交变化和检查结果总览开始。
- 分析、建议与商品按菌门分析单元组织，不再有报告级整体分析解释或总体建议：无命中的菌门允许为空，有命中的菌门必须单独确认，数据、物种或有效范围变化会使旧分析失效并阻断提交。规则目标从字典选择菌门或菌属（临时口径），条件只用状态类条件，输出「分析」「建议」加风险级别（低 / 中 / 高 / 仅提示）；规则菜单只维护规则和只读的按菌门规则测试，逐份运行和确认归入报告工作台。
- 已发布报告的更正草稿不替换当前线上版本，重新审核发布后才切换用户可见内容。
- 同一已发布报告最多存在一个活动更正草稿。
- 平台当前及长期只支持微生物组报告，不建设报告类型配置或通用报告引擎。
- 综合概览和有效检测结果是必备核心内容；综合概览需要宠物、报告物种、人工等级和综合分，有效结果至少包含一项有效数值或明确未检出。菌门建议与商品推荐不阻断发布，存在未确认的菌门分析单元则阻断。
- 没有任何有效参考范围时仍可发布：对比模块整块隐藏，菌门 / 菌属卡不显示正常范围，也不拼「略显稀疏 / 生机适宜 / 略显繁茂」。设计稿中的 2%–8%、12%–20%、15%–30%、40%–65% 不得进入生产。不允许在单份报告里手填第三套范围。平台范围方案是后续增强，不是出报告的前提。具体分类详情只展示有结果或明确未检出且被选中的项目；分析模块的最终发布条件仍待甲方确认（Q11）。
- 报告首屏摘要固定为 Alpha 多样性、均匀度和丰富度。v1 闭环：工作台可补录这三项；不填则原位置显示「暂无有效结果」，不换成别的指标，也不单独阻断发布。三个位置固定。以后是否改成机构给或公式算见 open-questions.md C2，不挡出报告。
- 平台评估维度当前固定为情绪 / 免疫两项，由审核在综合评定手填（C3）；报告首屏四个漂浮气泡含义仍待确认（Q5）。菌群科普模板保留设计稿字段，提示句不随检测状态变化。
- 商品推荐完全由运营人工配置：每个菌门分析单元最多选择一个主推 SPU，并可选配、手工排序最多三个关联推荐 SPU；「仅提示」风险的菌门不配商品；不使用健康标签、推荐映射、自动排序或自动替换。
- 商品配置与专业报告版本分开；运营保存商品调整后只更新该报告当前的小程序展示，不创建报告更正版本，也不影响其他报告。
- 每个菌门只展示一个主推商品卡（当前口径在菌门详情「总体建议」tab 下）；推荐详情顶部展示原主推商品，下方展示运营人工配置的关联推荐商品，不保存推荐理由。
- 可购商品点击后直接进入商城 SPU 详情；下架、缺货或已不存在的商品保留状态展示，系统不自动把其他商品提升为主推，由运营决定是否更换。
- 报告不保存商品信息快照，商品卡读取商城持续保留的当前或历史商品资料。首期不记录点击或交易归因。
- 管理端商品选择是高频操作，应提供分页、商品名称与编号搜索、分类及状态筛选，并允许查看在售、下架、缺货和回收站商品；已逻辑删除商品只保留既有关联展示，不重新进入搜索选择池。
- 有效参考范围只能来自审核人员确认可用的报告导入范围或匹配的平台配置；系统不校验导入范围的专业有效性，也不允许单份报告手填第三套范围。无范围不是发布阻断项。
- 平台范围方案是后续增强：未建设前，未发布工作版本也不会用设计稿数字冒充范围。若后续启用平台范围，未发布工作版本打开时采用最新匹配结果并显示来源、方案和版本，范围变化会触发重新分析确认，已发布版本不随平台配置变化。
- 平台范围只维护正常下限和正常上限，不增加参考依据说明、单侧范围或理想目标字段。
- 原始 Excel 和首次解析事实永久保留；人工补录或修订只更新当前有效结果并记录人工来源和当前修改原因，不保存逐次值变更链。
- 用户端不提供报告领取；自送检登记起至报告发布前，小程序统一显示不可点击的「报告处理中」，不暴露导入异常、待完善、待审核或驳回等内部状态。不提供报告旧版本、跨报告趋势或资料编辑。用户端也不提供宠物新建、编辑、认领或分享。
- v1 实施壳已锁定：在现有商城小程序同一 AppID 内新增健康报告模块，复用商城既有登录态、商品详情、购物车、订单、支付与退款能力；不新建独立报告小程序，不做跨小程序跳转，也不另建支付后端。小程序启动/登录默认进入健康报告首页（当前宠物页），主导航按原型为「首页」「我的」。即使一宠一份报告也不自动打开报告。健康报告首页不是看板、也不是第二份宠物列表：问候语用宠物档案名；一只宠物时主区块为不可点击「报告处理中」或进入该宠最新已发布报告，其下是该宠历史已发布报告的同一份列表；多宠时头部紧凑切换，切换即替换页面内容，没有完整宠物列表页；无宠物时显示「暂无宠物，请联系门店」。报告从健康报告首页打开为独立阅读页。

## Evidence on Hand

- 小程序视觉原型：`docs/product-design/img/`
- 完整业务设计：`docs/product-design/pet-health-report-business-design.md`
- 甲方核对清单：`docs/product-design/open-questions.md`
- 领域语言：`CONTEXT.md`
- 可交互静态原型：`docs/.vuepress/public/prototype/`
- 三份真实机构 Excel：`docs/product-design/report/`（harley / oscar / riley）

没有生产接口、真实微信用户数据或专业规则验收证据；不得把 Mock 演示推断为生产事实。

## Product Principles

- 身份在平台生成，Excel 只负责检测事实。
- 原始检测事实不可覆盖，人工修订和发布内容必须可追溯。
- 送检记录与报告是前后相接的两个业务对象：送检管理报告生成前履约，报告中心管理生成后的内容生命周期。
- 主状态保持少而稳定，具体处理问题在单份报告工作台中解决。
- 管理端围绕单份报告保持上下文，用户端只展示当前有效发布版本。
- 专业数值、分析与建议属于同一份报告并按菌门组织，不拆成两套报告，也不设报告级单例文案。
- 商城拥有商品与交易事实；健康报告模块在同一商城小程序内复用商品详情、购物车、订单、支付和退款能力，报告工作台只保存运营人工选择的 SPU 关联与展示顺序。
